=labelled_form_for [:admin, @widget], :url => admin_widgets_path do |f|
	
	#content_body.content_body
		- @page_title = "New widget | Widget Manager"
		%h1 Create new widget
		=flash_message

		=note "Fill in the form below to create a new widget"

		=render :partial=>"form", :locals=>{:f=>f}
	
	#content_sidebar
		=render :partial => "sidebar", :locals=>{:f=>f}
